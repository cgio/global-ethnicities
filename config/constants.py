# Output directory
PATH_OUTPUT = 'output'

# File name of master output file (exclude file extension)
FILE_OUTPUT = 'ethnicities'

# Local sources directory should contain .txt files
PATH_SOURCES_LOCAL = 'sources_local'

# Remote sources directory should contain .py files
PATH_SOURCES_REMOTE = 'sources_remote'
